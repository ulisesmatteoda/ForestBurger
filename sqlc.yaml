version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/init.sql"    # ← acá le decís dónde está el schema
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true