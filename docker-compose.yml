database:
  image: postgres:16
  environment:
    - POSTGRES_DB=${POSTGRES_DB}
    - POSTGRES_USER=${POSTGRES_USER}
    - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  volumes:
    - postgres_data:/var/lib/postgresql/data
    - ./db/shema.sql:/docker-entrypoint-initdb.d/
  ports:
    - "5432:5432"
volumes:

postgres_data:
